# EMS portal

## 📝 Project Description
this a project of ems portwal which is build in mern stack . in this project the tech stack is mern alson with react redux for state managemnet and some places there is also context api. and then we have used material ui and also we have used tailwind css for styling.also for backend we have use all those nasty packages which are to be used like express and mongoose . 

## 📁 Project Structure

the project is built in foloowing structure.
-vite_client ---> frontend of the project
vite_client >src >
---

## 🔐 Environment Variables

> List and describe required `.env` variables.

- `PORT=`  
- `MONGO_URI=`  
- `JWT_SECRET=`  
- `EMAIL_HOST=`  
- `EMAIL_PORT=`  
- `EMAIL_USER=`  
- `EMAIL_PASS=`  

---

## ✅ Key Takeaways

> Mention any important notes or behaviors from environment config.

- Secure sensitive values using `.env`  
- Set `CLIENT_URL` for CORS properly  
- Use separate values for dev and prod  

---

## 🚀 Future Expansion

> Ideas to improve or scale this project in the future.

- Add role-based access control  
- Introduce real-time features  
- Use Docker for deployment  
- Add testing and CI/CD  

